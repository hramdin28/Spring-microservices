spring:
  application:
    name: consumer
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      username: root
      password: s3cr3t

clients:
  producer:
    name: producer
